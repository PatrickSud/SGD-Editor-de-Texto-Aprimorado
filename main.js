/**
 * @file main.js — DESCONTINUADO
 *
 * Este arquivo foi descontinuado. O conteúdo foi redistribuído para:
 *   config.js     — FEATURE_SUGERIR_SAM e constantes SGD_*
 *   editor-ui.js  — editor de texto: toolbar, instância, saudações, histórico
 *   app.js        — nível de página: observers, FAB, cronômetro, notificações
 *                   (inclui a chamada initializeExtension() ao final)
 *
 * Este arquivo não está listado em manifest.json e não é carregado.
 */
